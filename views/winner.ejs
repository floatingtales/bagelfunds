<%- include('partials/header', {title:`History - Bagelfunds!`})%> <%-
include('partials/navbarUser') %>

<div class="bg-bagel container-fluid min-vh-100">
  <div class="row min-vh-100 py-2">
    <div
      class="col-lg-12 d-flex align-items-center justify-content-center font-override"
    >
      <div
        class="container text-center bg-secondary rounded-3 text-light bg-opacity-50"
      >
        <h1 class="my-3">Winner Winner Chicken Dinner!</h1>
        <div class="width-50">
          <img
            src="/<%= winningUser.profile_url %>"
            alt="<%= winningUser.username %> "
          />
        </div>
        <p>Congrats <%= winningUser.username %>! Now go eat a bagel!</p>
      </div>
    </div>
  </div>
</div>

<%-include('partials/footer') %>
